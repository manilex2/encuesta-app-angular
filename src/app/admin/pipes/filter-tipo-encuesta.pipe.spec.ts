import { FilterTipoEncuestaPipe } from './filter-tipo-encuesta.pipe';

describe('FilterTipoEncuestaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTipoEncuestaPipe();
    expect(pipe).toBeTruthy();
  });
});
